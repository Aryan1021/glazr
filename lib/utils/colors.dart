import 'package:flutter/material.dart';

class AppColors {
  // Glass Colors - Simple and working
  static const Color glassTint = Color.fromRGBO(255, 255, 255, 0.15);
  static const Color glassBorder = Color.fromRGBO(255, 255, 255, 0.3);
  static const Color glassHighlight = Color.fromRGBO(255, 255, 255, 0.1);

  // Text Colors
  static const Color primaryText = Colors.white;
  static const Color secondaryText = Color(0xFFB0BEC5);
  static const Color accentText = Color(0xFF64FFDA);
}